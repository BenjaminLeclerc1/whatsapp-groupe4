version: '3.8'

services:
  api-gateway:
    build:
      context: .
      dockerfile: ./cmd/api-gateway/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - USER_SERVICE_URL=http://user-service:8081
      - MESSAGE_SERVICE_URL=http://message-service:8082
      - PRESENCE_SERVICE_URL=http://presence-service:8083
      - SEARCH_SERVICE_URL=http://search-service:8084
    depends_on:
      - user-service
      - message-service
      - presence-service
      - search-service
    networks:
      - whatsapp-network

  user-service:
    build:
      context: .
      dockerfile: ./cmd/user-service/Dockerfile
    ports:
      - "8081:8081"
    environment:
      - PORT=8081
    networks:
      - whatsapp-network

  message-service:
    build:
      context: .
      dockerfile: ./cmd/message-service/Dockerfile
    ports:
      - "8082:8082"
    environment:
      - PORT=8082
    networks:
      - whatsapp-network

  presence-service:
    build:
      context: .
      dockerfile: ./cmd/presence-service/Dockerfile
    ports:
      - "8083:8083"
    environment:
      - PORT=8083
    networks:
      - whatsapp-network

  search-service:
    build:
      context: .
      dockerfile: ./cmd/search-service/Dockerfile
    ports:
      - "8084:8084"
    environment:
      - PORT=8084
    networks:
      - whatsapp-network

networks:
  whatsapp-network:
    driver: bridge
